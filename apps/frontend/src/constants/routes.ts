const routes = {
    HOME: '/folder/root',
    LOGIN: '/auth/login',
    REGISTER: '/auth/register',
    RESET_PASSWORD: '/auth/reset-password',
    DASHBOARD: '/dashboard',
    SEARCH: '/search',
}

export default routes;